<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view"
    android:id="@+id/tool_bar_menu"
    >
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/btn_arithmetics"
            android:title="arithmetics"
            android:icon="@drawable/ic_arithmetics" />
        <item
            android:id="@+id/btn_binary"
            android:title="binary"
            android:icon="@drawable/ic_binary" />
        <item
            android:id="@+id/btn_date"
            android:title="date"
            android:icon="@drawable/ic_date"/>
        <item
            android:id="@+id/btn_data"
            android:title="data"
            android:icon="@drawable/ic_data" />
    </group>
</menu>